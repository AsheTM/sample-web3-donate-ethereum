import { AppHomeDisableDonatePipe } from './app-home-disable-donate.pipe';

describe('AppHomeDisableDonatePipe', () => {
  it('create an instance', () => {
    const pipe = new AppHomeDisableDonatePipe();
    expect(pipe).toBeTruthy();
  });
});
