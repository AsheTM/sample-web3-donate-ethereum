import { SharedWeb3OutletDirective } from './shared-web3-outlet.directive';

describe('SharedWeb3OutletDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedWeb3OutletDirective();
    expect(directive).toBeTruthy();
  });
});
