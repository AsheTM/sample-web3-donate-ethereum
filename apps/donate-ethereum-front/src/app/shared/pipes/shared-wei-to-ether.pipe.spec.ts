import { SharedWeiToEtherPipe } from './shared-wei-to-ether.pipe';

describe('SharedWeiToEtherPipe', () => {
  it('create an instance', () => {
    const pipe = new SharedWeiToEtherPipe();
    expect(pipe).toBeTruthy();
  });
});
